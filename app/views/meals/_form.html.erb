<%= form_for([@user, @entry, @meal], remote: true) do |f| %>
  <%= f.label :name, "Meal" %><br>
  <%= f.text_field :name %><br>
  <%= f.label :calorie_count %><br>
  <%= f.text_field :calorie_count %><br>
  <%= f.submit "Add" %>
<% end %>
